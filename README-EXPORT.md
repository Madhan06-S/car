# Preset site export

Template: targo-logistics

## Quick deploy (pre-built)
Upload this folder — index.html loads your copy/theme via draftly-custom/.

## Rebuild from source
```bash
cd targo-logistics
npm install
npm run build
```
Then deploy the dist output and keep the draftly-custom/ folder alongside index.html.